<!DOCTYPE html>
<html>
  <head>
    <title>SocialTeeth</title>
    <%= @pinion.css_url("/css/common.css") %>
    <%= @pinion.js_url("/js/lib/jquery-1.7.2.min.js") %>
    <%= yield_content :head %>
  </head>
  <body>
    <% unless defined?(render_header) && !render_header %>
      <div id="header">
        <div class="container">
          <a id="logo" href="/">SocialTeeth</a>
          <nav>
            <ul>
              <li><a href="/browse">Browse Ads</a></li>
              <li><a href="/submit">Submit Ad</a></li>
              <li><a href="/about">About</a></li>
            </ul>
          </nav>
          <div id="signin">
            <% if current_user %>
              <a href="#"><%= current_user.name %></a> |
              <a href="/signout">Sign Out</a>
            <% else %>
              <a href="/signup">Sign Up</a> |
              <a href="/signin">Sign In</a>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
    <div id="content">
      <%= yield %>
    </div>
  </body>
</html>

